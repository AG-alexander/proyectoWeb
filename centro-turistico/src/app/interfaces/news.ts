export interface News {
    idNews?: number; // identificacion
    title: string;// titulo
    content: string; // contenido
    date: Date; // fecha
    image: string; // imagen
    id?: string; // firebase id
}
